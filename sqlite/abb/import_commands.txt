DROP TABLE IF EXISTS devices;

create table devices (
  id number primary key,
  name text,
  price number,
  a_min number,
  a_max number,
  type text,
  poles text,
  filter text
);

.mode csv
.import abb.csv devices --skip 1
